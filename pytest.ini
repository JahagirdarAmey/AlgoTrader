# pytest.ini
[pytest]
bdd_features_base_dir = tests/features
addopts = -v -rx -l --tb=short
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
testpaths = tests

# requirements.txt
pytest==7.4.3
pytest-bdd==7.0.1
pandas==2.1.1
numpy==1.24.3